{
  "name": "e-circuit",
  "version": "0.0.1",
  "description": "Electric Circuit",
  "author": "tgfjt",
  "license": "MIT",
  "scripts": {
    "buildjs": "watchify src/js/app.js -o public/js/bundle.js",
    "buildcss": "node-sass src/styles/ -w -o public/css",
    "watch": "npm run buildjs & npm run buildcss",
    "start": "npm run watch & node server.js & open http://localhost:8080"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "ecstatic": "^1.4.0",
    "node-sass": "^3.4.2",
    "watchify": "^3.6.1"
  },
  "dependencies": {
    "domready": "^1.0.8",
    "raf": "^3.1.0"
  }
}
